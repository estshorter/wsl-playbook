  - name: Install packages using apt
    become: yes
    apt:
      name: 
        - language-pack-ja
        - build-essential
        - clang
        - cmake
        - ninja-build
        - pkg-config
        - wget
        - curl
        - unzip
